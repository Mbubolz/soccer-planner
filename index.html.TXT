<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pro Soccer Session Planner & Club Suite</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        darkMode: 'class',
        theme: {
          extend: {
            colors: {
              emerald: { 400: '#34d399', 500: '#10b981', 600: '#059669', 700: '#047857', 950: '#022c22' }
            }
          }
        }
      }
    </script>
    <!-- React & ReactDOM v18 -->
    <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <!-- Babel Standalone -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <style>
      body { background-color: #030712; color: #f3f4f6; font-family: system-ui, -apple-system, sans-serif; }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect, useRef } = React;

        function App() {
          const [activeTab, setActiveTab] = useState('sheet');
          const [errorState, setErrorState] = useState(null);
          const [showWelcomeModal, setShowWelcomeModal] = useState(true);

          useEffect(() => {
            const handleError = (event) => {
              setErrorState(event.message || 'An unexpected error occurred in the application.');
            };
            window.addEventListener('error', handleError);
            return () => window.removeEventListener('error', handleError);
          }, []);

          const [sheetData, setSheetData] = useState({
            clubName: 'ACADEMY FC ELITE',
            coachName: 'Coach Alex',
            ageGroup: 'U-19 Pro Academy',
            sessionTitle: 'Building Out Under High Press & Finding The Third Man',
            date: new Date().toISOString().split('T')[0],
            focus: 'Positional Superiority & Line Breaking',
            rpe: 8,
            duration: 90,
            manageOneself: '1. Scanning behind shoulder before receiving\n2. Open body orientation to receive on half-turn\n3. Decisive 1-2 touch actions under pressure',
            manageSpace: '1. Creating numerical superiority in central corridors\n2. Pinning opposition fullbacks wide with wingers\n3. Rotation between CM and inverted fullback',
            manageTheBall: '1. Firm pass into the front foot with backspin\n2. Disguise passing angle to freeze defensive block\n3. Immediate counter-press within 2 seconds of turnover'
          });

          const [activities, setActivities] = useState([
            {
              id: 1,
              title: 'Warm-up: 4v2 Rondo with Directional Transition',
              tags: ['Activation', 'Passing', 'Scanning'],
              duration: '15 mins',
              setsReps: '3 sets x 4 mins (1 min rest)',
              intensity: 'Medium',
              explanation: '4 attackers retain possession against 2 defenders inside a 15x15 grid. Upon completing 8 passes, ball must be switched to target zone.',
              coachNotes: 'Encourage 1-2 touch play. Demand scanning before ball arrives.',
              progression: 'Reduce grid size to 12x12 and mandate 1-touch passing.',
              collapsed: false,
              canvasElements: [],
              viewMode: 'third',
              snapGrid: true
            },
            {
              id: 2,
              title: 'Functional Build-Up: 3+GK vs 2 CB/CM Structure',
              tags: ['Tactical', 'Build-up', 'Positional'],
              duration: '25 mins',
              setsReps: '4 blocks x 5 mins',
              intensity: 'High',
              explanation: 'GK and 2 Center Backs build against 2 opposing forwards. Defensive midfielder drops deep to create 4v2 numerical overload.',
              coachNotes: 'Center backs split wide to penalty box edges. GK acts as permanent free sweeper.',
              progression: 'Add opposing central midfielder to create 4v3 situational overload.',
              collapsed: false,
              canvasElements: [],
              viewMode: 'half',
              snapGrid: true
            },
            {
              id: 3,
              title: 'Tactical Game: 8v8 + 2 Neutral Zones',
              tags: ['Match Scenario', 'Third-Man', 'Pressing'],
              duration: '35 mins',
              setsReps: '2 halves x 15 mins (5 min HT)',
              intensity: 'Very High',
              explanation: '8v8 match simulation on 3/4 pitch with two wide neutral channels. Goals scored from third-man combinations count double.',
              coachNotes: 'Enforce maximum 2 touches in central corridor. Exploit wide spaces quickly.',
              progression: 'Remove neutral player restriction once team achieves 5 successful line breaks.',
              collapsed: false,
              canvasElements: [],
              viewMode: 'full',
              snapGrid: true
            },
            {
              id: 4,
              title: 'Regeneration & Tactical Debrief',
              tags: ['Cool Down', 'Analysis', 'Mobility'],
              duration: '15 mins',
              setsReps: '1 block',
              intensity: 'Low',
              explanation: 'Low-intensity dynamic mobility drills paired with tactical review of key line-breaking moments from session.',
              coachNotes: 'Focus on static hamstring and hip flexor stretching. Q&A on principles.',
              progression: 'N/A',
              collapsed: false,
              canvasElements: [],
              viewMode: 'third',
              snapGrid: true
            }
          ]);

          const [drillBank, setDrillBank] = useState([
            { id: 101, title: 'Box-to-Box Transition 4v4+3', category: 'Transition', duration: '20m', intensity: 'High', explanation: 'Fast vertical transitions after winning possession in middle third.' },
            { id: 102, title: 'Defensive Block Discipline 6v4', category: 'Defense', duration: '20m', intensity: 'Medium', explanation: 'Maintain compact defensive lines and shifting mechanics against overload.' },
            { id: 103, title: 'Final Third Overlaps & Cutbacks', category: 'Attacking', duration: '25m', intensity: 'High', explanation: 'Winger and fullback combination leading to cross and box penetration.' }
          ]);

          const [sessionLibrary, setSessionLibrary] = useState([
            {
              id: 1001,
              savedAt: '2026-03-01',
              sheetData: {
                clubName: 'ACADEMY FC ELITE',
                sessionTitle: 'Compact Defensive Block & Low-Block Recovery',
                ageGroup: 'U-19 Pro Academy',
                duration: 90
              },
              activitiesCount: 4
            }
          ]);

          const [calendarNotes, setCalendarNotes] = useState({});
          const [selectedCalendarDate, setSelectedCalendarDate] = useState(new Date().toISOString().split('T')[0]);

          const [roster, setRoster] = useState([
            { id: 1, name: 'Lucas Silva', number: 1, pos: 'GK', status: 'Available', idp: 'Improve distribution under high press' },
            { id: 2, name: 'Marcus Vance', number: 2, pos: 'RB', status: 'Available', idp: 'Timing of overlapping runs' },
            { id: 3, name: 'Mateo Fernandez', number: 4, pos: 'CB', status: 'Available', idp: 'Vertical progressive passing' },
            { id: 4, name: 'Stefan Savic', number: 5, pos: 'CB', status: 'Available', idp: 'Aerial duel dominance' },
            { id: 5, name: 'Theo Hernandez', number: 3, pos: 'LB', status: 'Available', idp: '1v1 defensive containment' },
            { id: 6, name: 'Declan Rice', number: 6, pos: 'CDM', status: 'Available', idp: 'Scan space before receiving' },
            { id: 7, name: 'Pedri Gonzalez', number: 8, pos: 'CM', status: 'Available', idp: 'Half-turn body orientation' },
            { id: 8, name: 'Jude Bellingham', number: 10, pos: 'CAM', status: 'Available', idp: 'Third-man run timing' },
            { id: 9, name: 'Vinicius Jr', number: 11, pos: 'LW', status: 'Available', idp: 'Final third cutback execution' },
            { id: 10, name: 'Kylian Mbappe', number: 9, pos: 'ST', status: 'Available', idp: 'Exploiting shoulder of last defender' },
            { id: 11, name: 'Bukayo Saka', number: 7, pos: 'RW', status: 'Available', idp: 'Inverted diagonal runs' }
          ]);

          const [loadMetrics] = useState({
            acuteWorkload: 720,
            chronicWorkload: 650,
            monotonyIndex: 1.4,
            strainIndex: 1008,
            weeklyDistanceKm: 34.5,
            highSpeedRunningM: 3200
          });

          const [matchdayXI, setMatchdayXI] = useState({
            formation: '4-3-3',
            cornerRoutine: 'Inswinging delivery to penalty spot with front-post decoy block',
            freeKickRoutine: 'Direct shot option with secondary wall runner distraction'
          });

          const [microcycle] = useState({
            Monday: { type: 'Active Recovery & Gym', load: 'Low', notes: 'Mobility, core stability & regeneration' },
            Tuesday: { type: 'Tactical Strength & Build-up', load: 'High', notes: 'Focus on Manage Space principles' },
            Wednesday: { type: 'High Intensity Match Integration', load: 'Very High', notes: '11v11 tactical simulation' },
            Thursday: { type: 'Technical Sharpness', load: 'Medium', notes: 'Passing combinations & finishing' },
            Friday: { type: 'Match Prep & Set Pieces', load: 'Low', notes: 'Tactical walkthrough & dead balls' },
            Saturday: { type: 'MATCH DAY (League Fixture)', load: 'Max', notes: 'Execute club game model' },
            Sunday: { type: 'Complete Rest', load: 'None', notes: 'Mental and physical recovery' }
          });

          const [toast, setToast] = useState(null);

          const showToast = (message) => {
            setToast(message);
            setTimeout(() => setToast(null), 3500);
          };

          useEffect(() => {
            try {
              const savedLib = localStorage.getItem('pro_soccer_library');
              if (savedLib) setSessionLibrary(JSON.parse(savedLib));
              const savedBank = localStorage.getItem('pro_soccer_drillbank');
              if (savedBank) setDrillBank(JSON.parse(savedBank));
              const savedNotes = localStorage.getItem('pro_soccer_cal_notes');
              if (savedNotes) setCalendarNotes(JSON.parse(savedNotes));
              const savedRoster = localStorage.getItem('pro_soccer_roster');
              if (savedRoster) setRoster(JSON.parse(savedRoster));
            } catch (e) {
              console.error('LocalStorage load error:', e);
            }
          }, []);

          const saveToLocalStorage = () => {
            try {
              localStorage.setItem('pro_soccer_library', JSON.stringify(sessionLibrary));
              localStorage.setItem('pro_soccer_drillbank', JSON.stringify(drillBank));
              localStorage.setItem('pro_soccer_cal_notes', JSON.stringify(calendarNotes));
              localStorage.setItem('pro_soccer_roster', JSON.stringify(roster));
              showToast('All app data & pro integrations successfully saved!');
            } catch (e) {
              showToast('Error saving data.');
            }
          };

          if (errorState) {
            return (
              <div className="min-h-screen bg-slate-950 text-white p-10 flex flex-col items-center justify-center">
                <div className="bg-red-950/80 border border-red-800 p-6 rounded-2xl max-w-lg text-center space-y-4">
                  <h2 className="text-xl font-bold">Application Error Encountered</h2>
                  <p className="text-sm text-red-200">{errorState}</p>
                  <button onClick={() => { setErrorState(null); window.location.reload(); }} className="bg-red-600 px-4 py-2 rounded-lg text-sm font-bold hover:bg-red-500">
                    Reload Application
                  </button>
                </div>
              </div>
            );
          }

          const TacticalCanvas = ({ activityIndex, initialViewMode }) => {
            const canvasRef = useRef(null);
            const activity = activities[activityIndex] || { canvasElements: [] };
            const [tool, setTool] = useState('player_blue');
            const [viewMode, setViewMode] = useState(initialViewMode || 'half');
            const [elements, setElements] = useState(activity.canvasElements || []);
            const [isDrawing, setIsDrawing] = useState(false);
            const [currentPath, setCurrentPath] = useState([]);
            const [isPlaying, setIsPlaying] = useState(false);
            const [animFrame, setAnimFrame] = useState(0);

            useEffect(() => {
              redrawCanvas();
            }, [elements, viewMode, currentPath, animFrame]);

            useEffect(() => {
              let timer;
              if (isPlaying) {
                timer = setInterval(() => {
                  setAnimFrame(prev => (prev + 1) % 180);
                }, 30);
              }
              return () => clearInterval(timer);
            }, [isPlaying]);

            const updateElementsInState = (newEls) => {
              setElements(newEls);
              const updatedActs = [...activities];
              if (updatedActs[activityIndex]) {
                updatedActs[activityIndex].canvasElements = newEls;
                setActivities(updatedActs);
              }
            };

            const redrawCanvas = () => {
              const canvas = canvasRef.current;
              if (!canvas) return;
              const ctx = canvas.getContext('2d');
              ctx.clearRect(0, 0, canvas.width, canvas.height);

              const grad = ctx.createLinearGradient(0, 0, 0, canvas.height);
              grad.addColorStop(0, '#153122');
              grad.addColorStop(1, '#1e3a2f');
              ctx.fillStyle = grad;
              ctx.fillRect(0, 0, canvas.width, canvas.height);

              ctx.strokeStyle = 'rgba(255, 255, 255, 0.85)';
              ctx.lineWidth = 2;

              const w = canvas.width;
              const h = canvas.height;

              if (viewMode === 'full') {
                ctx.strokeRect(40, 40, w - 80, h - 80);
                ctx.beginPath(); ctx.moveTo(w / 2, 40); ctx.lineTo(w / 2, h - 40); ctx.stroke();
                ctx.beginPath(); ctx.arc(w / 2, h / 2, 60, 0, Math.PI * 2); ctx.stroke();
              } else if (viewMode === 'half') {
                ctx.strokeRect(40, 40, w - 80, h - 80);
                ctx.strokeRect(w / 2 - 140, h - 140, 280, 100);
              } else {
                ctx.strokeRect(40, 40, w - 80, h - 80);
              }

              elements.forEach((el, idx) => {
                ctx.save();
                let renderX = el.x;
                let renderY = el.y;

                if (isPlaying && (el.type === 'player_blue' || el.type === 'player_red')) {
                  renderX += Math.sin((animFrame + idx * 30) / 25) * 22;
                  renderY += Math.cos((animFrame + idx * 20) / 25) * 15;
                }

                if (el.type === 'player_blue' || el.type === 'player_red') {
                  ctx.beginPath();
                  ctx.arc(renderX, renderY, 16, 0, Math.PI * 2);
                  ctx.fillStyle = el.type === 'player_blue' ? '#3b82f6' : '#ef4444';
                  ctx.fill();
                  ctx.strokeStyle = '#ffffff';
                  ctx.lineWidth = 2.5;
                  ctx.stroke();

                  ctx.fillStyle = '#ffffff';
                  ctx.font = 'bold 12px sans-serif';
                  ctx.textAlign = 'center';
                  ctx.textBaseline = 'middle';
                  ctx.fillText(el.number || '10', renderX, renderY);
                } else if (el.type === 'ball') {
                  if (isPlaying) {
                    renderX += Math.cos(animFrame / 15) * 35;
                    renderY += Math.sin(animFrame / 15) * 25;
                  }
                  ctx.beginPath();
                  ctx.arc(renderX, renderY, 8, 0, Math.PI * 2);
                  ctx.fillStyle = '#ffffff';
                  ctx.fill();
                  ctx.stroke();
                } else if (el.path && el.path.length > 1) {
                  ctx.beginPath();
                  ctx.moveTo(el.path[0].x, el.path[0].y);
                  for (let i = 1; i < el.path.length; i++) {
                    ctx.lineTo(el.path[i].x, el.path[i].y);
                  }
                  ctx.strokeStyle = '#ffffff';
                  ctx.lineWidth = 3;
                  ctx.stroke();
                }
                ctx.restore();
              });

              if (currentPath.length > 1) {
                ctx.beginPath();
                ctx.moveTo(currentPath[0].x, currentPath[0].y);
                for (let i = 1; i < currentPath.length; i++) {
                  ctx.lineTo(currentPath[i].x, currentPath[i].y);
                }
                ctx.strokeStyle = '#ffffff';
                ctx.lineWidth = 3;
                ctx.stroke();
              }
            };

            const handleMouseDown = (e) => {
              const canvas = canvasRef.current;
              if (!canvas) return;
              const rect = canvas.getBoundingClientRect();
              let x = e.clientX - rect.left;
              let y = e.clientY - rect.top;

              if (tool === 'eraser') {
                const filtered = elements.filter(el => {
                  if (el.x && el.y) {
                    const dist = Math.hypot(el.x - x, el.y - y);
                    return dist > 25;
                  }
                  return true;
                });
                updateElementsInState(filtered);
                showToast('Erased element at position');
              } else if (tool === 'arrow_pass') {
                setIsDrawing(true);
                setCurrentPath([{ x, y }]);
              } else {
                const num = (tool === 'player_blue' || tool === 'player_red') ? prompt('Enter shirt number:', '7') : null;
                updateElementsInState([...elements, { type: tool, x, y, number: num || '11' }]);
              }
            };

            const handleMouseMove = (e) => {
              if (!isDrawing || !canvasRef.current) return;
              const rect = canvasRef.current.getBoundingClientRect();
              setCurrentPath(prev => [...prev, { x: e.clientX - rect.left, y: e.clientY - rect.top }]);
            };

            const handleMouseUp = () => {
              if (!isDrawing) return;
              setIsDrawing(false);
              if (currentPath.length > 1) {
                updateElementsInState([...elements, { path: currentPath }]);
              }
              setCurrentPath([]);
            };

            return (
              <div className="bg-slate-900 border border-slate-700 rounded-2xl p-4 shadow-xl text-white">
                <div className="flex flex-wrap items-center justify-between gap-2 mb-3 pb-3 border-b border-slate-800">
                  <div className="flex items-center gap-1.5 flex-wrap">
                    <button onClick={() => setTool('player_blue')} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold ${tool === 'player_blue' ? 'bg-blue-600 text-white' : 'bg-slate-800 text-slate-300'}`}>Blue</button>
                    <button onClick={() => setTool('player_red')} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold ${tool === 'player_red' ? 'bg-red-600 text-white' : 'bg-slate-800 text-slate-300'}`}>Red</button>
                    <button onClick={() => setTool('ball')} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold ${tool === 'ball' ? 'bg-emerald-600 text-white' : 'bg-slate-800 text-slate-300'}`}>Ball</button>
                    <button onClick={() => setTool('arrow_pass')} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold ${tool === 'arrow_pass' ? 'bg-indigo-600 text-white' : 'bg-slate-800 text-slate-300'}`}>Pass</button>
                    <button onClick={() => setTool('eraser')} className={`px-2.5 py-1.5 rounded-lg text-xs font-semibold ${tool === 'eraser' ? 'bg-rose-600 text-white' : 'bg-slate-800 text-slate-300'}`}>Eraser</button>
                  </div>
                  <div className="flex items-center gap-2">
                    <button onClick={() => setIsPlaying(!isPlaying)} className={`px-3 py-1.5 rounded-lg text-xs font-bold ${isPlaying ? 'bg-amber-600 text-white animate-pulse' : 'bg-slate-800 text-emerald-400'}`}>
                      {isPlaying ? 'Pause Animation' : '▶ Play Animation'}
                    </button>
                    <select value={viewMode} onChange={(e) => setViewMode(e.target.value)} className="bg-slate-800 text-xs border border-slate-700 rounded-lg px-2 py-1 text-white">
                      <option value="full">Full Field</option>
                      <option value="half">Half Field</option>
                      <option value="third">Third Field</option>
                    </select>
                    <button onClick={() => updateElementsInState([])} className="text-xs text-rose-400">Clear All</button>
                  </div>
                </div>
                <div className="flex justify-center bg-slate-950 p-2.5 rounded-xl border border-slate-800">
                  <canvas ref={canvasRef} width={680} height={380} onMouseDown={handleMouseDown} onMouseMove={handleMouseMove} onMouseUp={handleMouseUp} className="cursor-crosshair rounded-lg max-w-full h-auto" />
                </div>
              </div>
            );
          };

          return (
            <div className="min-h-screen bg-slate-950 text-slate-100 font-sans pb-20">
              {showWelcomeModal && (
                <div className="fixed inset-0 z-50 bg-slate-950/80 backdrop-blur-md flex items-center justify-center p-4">
                  <div className="bg-slate-900 border border-emerald-500/40 rounded-3xl max-w-lg w-full p-8 shadow-2xl text-center space-y-6 relative overflow-hidden">
                    <div className="absolute top-0 left-0 right-0 h-2 bg-gradient-to-r from-emerald-500 via-blue-500 to-indigo-500"></div>
                    <div className="w-16 h-16 bg-emerald-600/20 border border-emerald-500 rounded-2xl flex items-center justify-center mx-auto text-emerald-400 shadow-lg font-bold text-xl">⚽</div>
                    <div className="space-y-2">
                      <h2 className="text-2xl font-black tracking-wide text-white">PRO SOCCER SUITE</h2>
                      <p className="text-xs uppercase font-bold tracking-widest text-emerald-400">Elite Professional Academy Management</p>
                      <p className="text-sm text-slate-300 pt-2">Welcome coach. Your advanced tactical session planner, GPS load tracker, interactive pitch animator, and matchday suite are ready.</p>
                    </div>
                    <div className="pt-2">
                      <button onClick={() => setShowWelcomeModal(false)} className="w-full bg-emerald-600 hover:bg-emerald-500 text-white font-bold py-3.5 px-6 rounded-2xl shadow-xl transition transform hover:-translate-y-0.5 text-sm">
                        Enter Tactical Suite
                      </button>
                    </div>
                  </div>
                </div>
              )}

              {toast && (
                <div className="fixed top-5 right-5 z-50 bg-emerald-600 text-white px-4 py-3 rounded-xl shadow-2xl flex items-center gap-2.5 text-sm font-semibold animate-bounce">
                  ✓ {toast}
                </div>
              )}

              <header className="bg-slate-900 border-b border-slate-800 sticky top-0 z-40 px-6 py-3.5 flex flex-wrap items-center justify-between gap-4 shadow-lg">
                <div className="flex items-center gap-3">
                  <div className="bg-gradient-to-br from-emerald-500 to-emerald-700 p-2.5 rounded-xl text-white shadow-md font-bold">⚽</div>
                  <div>
                    <h1 className="font-black text-lg text-white tracking-wide">PRO SOCCER SUITE <span className="text-emerald-400 font-normal text-xs ml-2 px-2.5 py-0.5 bg-emerald-950/80 rounded-full border border-emerald-800">Elite Club Edition</span></h1>
                    <p className="text-xs text-slate-400">Session Planner, Roster Hub, GPS Analytics & Matchday Suite</p>
                  </div>
                </div>

                <div className="flex items-center gap-1.5 bg-slate-950 p-1.5 rounded-2xl border border-slate-800 overflow-x-auto">
                  <button onClick={() => setActiveTab('sheet')} className={`flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === 'sheet' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}>
                    Session
                  </button>
                  <button onClick={() => setActiveTab('roster')} className={`flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === 'roster' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}>
                    Squad Roster
                  </button>
                  <button onClick={() => setActiveTab('analytics')} className={`flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === 'analytics' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}>
                    GPS & Load
                  </button>
                  <button onClick={() => setActiveTab('matchday')} className={`flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === 'matchday' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}>
                    Matchday XI
                  </button>
                  <button onClick={() => setActiveTab('calendar')} className={`flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === 'calendar' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}>
                    Calendar
                  </button>
                  <button onClick={() => setActiveTab('microcycle')} className={`flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === 'microcycle' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}>
                    Microcycle
                  </button>
                  <button onClick={() => setActiveTab('bank')} className={`flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === 'bank' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}>
                    Drills
                  </button>
                  <button onClick={() => setActiveTab('library')} className={`flex items-center gap-1.5 px-3 py-2 rounded-xl text-xs font-bold transition ${activeTab === 'library' ? 'bg-emerald-600 text-white' : 'text-slate-400'}`}>
                    Library
                  </button>
                </div>

                <div className="flex items-center gap-2">
                  <button onClick={saveToLocalStorage} className="bg-emerald-600/20 hover:bg-emerald-600/30 text-emerald-400 border border-emerald-600/40 px-3 py-2 rounded-xl text-xs font-bold flex items-center gap-1.5">
                    Save
                  </button>
                  <button onClick={() => window.print()} className="bg-slate-800 hover:bg-slate-700 text-slate-200 px-3 py-2 rounded-xl text-xs font-semibold flex items-center gap-1.5 border border-slate-700">
                    Print
                  </button>
                </div>
              </header>

              <main className="max-w-7xl mx-auto px-4 py-6">

                {activeTab === 'sheet' && (
                  <div className="space-y-6">
                    <div className="bg-slate-900 border border-slate-800 rounded-3xl p-6 shadow-2xl space-y-6">
                      <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
                        <div>
                          <label className="block text-xs font-bold text-slate-400 mb-1.5">Club Name</label>
                          <input type="text" value={sheetData.clubName} onChange={e => setSheetData({...sheetData, clubName: e.target.value})} className="w-full bg-slate-950 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm font-bold text-emerald-400" />
                        </div>
                        <div>
                          <label className="block text-xs font-bold text-slate-400 mb-1.5">Coach Name</label>
                          <input type="text" value={sheetData.coachName} onChange={e => setSheetData({...sheetData, coachName: e.target.value})} className="w-full bg-slate-950 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-slate-200" />
                        </div>
                        <div>
                          <label className="block text-xs font-bold text-slate-400 mb-1.5">Age Group</label>
                          <input type="text" value={sheetData.ageGroup} onChange={e => setSheetData({...sheetData, ageGroup: e.target.value})} className="w-full bg-slate-950 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-slate-200" />
                        </div>
                        <div>
                          <label className="block text-xs font-bold text-slate-400 mb-1.5">Date</label>
                          <input type="date" value={sheetData.date} onChange={e => setSheetData({...sheetData, date: e.target.value})} className="w-full bg-slate-950 border border-slate-700 rounded-xl px-3.5 py-2.5 text-sm text-slate-200" />
                        </div>
                      </div>

                      <div className="grid grid-cols-1 md:grid-cols-3 gap-4 pt-4 border-t border-slate-800">
                        <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800 space-y-2">
                          <h4 className="text-xs font-black uppercase text-blue-400">1. Manage Oneself</h4>
                          <textarea rows="3" value={sheetData.manageOneself} onChange={e => setSheetData({...sheetData, manageOneself: e.target.value})} className="w-full bg-slate-900 border border-slate-800 rounded-xl p-2.5 text-xs text-slate-300"></textarea>
                        </div>
                        <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800 space-y-2">
                          <h4 className="text-xs font-black uppercase text-emerald-400">2. Manage Space</h4>
                          <textarea rows="3" value={sheetData.manageSpace} onChange={e => setSheetData({...sheetData, manageSpace: e.target.value})} className="w-full bg-slate-900 border border-slate-800 rounded-xl p-2.5 text-xs text-slate-300"></textarea>
                        </div>
                        <div className="bg-slate-950 p-4 rounded-2xl border border-slate-800 space-y-2">
                          <h4 className="text-xs font-black uppercase text-amber-400">3. Manage The Ball</h4>
                          <textarea rows="3" value={sheetData.manageTheBall} onChange={e => setSheetData({...sheetData, manageTheBall: e.target.value})} className="w-full bg-slate-900 border border-slate-800 rounded-xl p-2.5 text-xs text-slate-300"></textarea>
                        </div>
                      </div>
                    </div>

                    <div className="space-y-4">
                      <h2 className="text-xl font-black text-white">Training Session Blocks</h2>
                      {activities.map((act, index) => (
                        <div key={act.id} className="bg-slate-900 border border-slate-800 rounded-3xl shadow-2xl p-6 space-y-4">
                          <div className="flex justify-between items-center">
                            <h3 className="font-bold text-white text-base">{act.title}</h3>
                            <span className="text-xs bg-slate-800 px-3 py-1 rounded-lg text-emerald-400">{act.duration}</span>
                          </div>
                          <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                            <div className="space-y-3">
                              <textarea rows="3" value={act.explanation} onChange={e => {
                                const updated = [...activities];
                                updated[index].explanation = e.target.value;
                                setActivities(updated);
                              }} className="w-full bg-slate-950 border border-slate-800 rounded-xl p-3 text-xs text-slate-300"></textarea>
                              <textarea rows="3" value={act.coachNotes} onChange={e => {
                                const updated = [...activities];
                                updated[index].coachNotes = e.target.value;
                                setActivities(updated);
                              }} className="w-full bg-slate-950 border border-slate-800 rounded-xl p-3 text-xs text-slate-300"></textarea>
                            </div>
                            <div>
                              <TacticalCanvas activityIndex={index} initialViewMode={act.viewMode} />
                            </div>
                          </div>
                        </div>
                      ))}
                    </div>
                  </div>
                )}

                {activeTab === 'roster' && (
                  <div className="bg-slate-900 border border-slate-800 rounded-3xl p-6 shadow-2xl space-y-6">
                    <div className="flex justify-between items-center flex-wrap gap-4">
                      <div>
                        <h2 className="text-xl font-black text-white">Squad Roster & Individual Development Plans (IDPs)</h2>
                        <p className="text-xs text-slate-400">Manage athlete profiles, availability status, and targeted technical IDP goals.</p>
                      </div>
                      <button onClick={() => {
                        const name = prompt('Player Name:');
                        if (!name) return;
                        const newPlayer = { id: Date.now(), name, number: 99, pos: 'CM', status: 'Available', idp: 'First touch under pressure' };
                        setRoster([...roster, newPlayer]);
                        showToast("Player added to roster!");
                      }} className="bg-emerald-600 hover:bg-emerald-500 text-white text-xs font-bold px-4 py-2.5 rounded-xl shadow flex items-center gap-1.5">
                        + Add Athlete
                      </button>
                    </div>

                    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                      {roster.map((player, idx) => (
                        <div key={player.id} className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-3">
                          <div className="flex justify-between items-center">
                            <span className="bg-emerald-950 text-emerald-400 border border-emerald-800 text-xs font-extrabold px-2.5 py-1 rounded-lg">
                              #{player.number} • {player.pos}
                            </span>
                            <select value={player.status} onChange={e => {
                              const updated = [...roster];
                              updated[idx].status = e.target.value;
                              setRoster(updated);
                            }} className={`text-[10px] font-bold px-2 py-1 rounded-md border ${
                              player.status === 'Available' ? 'bg-emerald-950 text-emerald-400 border-emerald-800' : 'bg-rose-950 text-rose-400 border-rose-800'
                            }`}>
                              <option value="Available">Available</option>
                              <option value="Injured">Injured</option>
                              <option value="Sick">Sick</option>
                              <option value="National Duty">National Duty</option>
                            </select>
                          </div>
                          <div>
                            <h3 className="font-bold text-white text-base">{player.name}</h3>
                          </div>
                          <div>
                            <label className="text-[10px] uppercase font-bold text-slate-400 block mb-1">Individual Development Plan (IDP)</label>
                            <input type="text" value={player.idp} onChange={e => {
                              const updated = [...roster];
                              updated[idx].idp = e.target.value;
                              setRoster(updated);
                            }} className="w-full bg-slate-900 border border-slate-800 rounded-xl px-3 py-2 text-xs text-slate-200" />
                          </div>
                        </div>
                      ))}
                    </div>
                  </div>
                )}

                {activeTab === 'analytics' && (
                  <div className="bg-slate-900 border border-slate-800 rounded-3xl p-6 shadow-2xl space-y-6">
                    <div>
                      <h2 className="text-xl font-black text-white">Live Sports Science & GPS Analytics Dashboard</h2>
                      <p className="text-xs text-slate-400">Monitor acute-to-chronic workload ratio, training monotony, and overtraining injury risk indices.</p>
                    </div>

                    <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
                      <div className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-2">
                        <span className="text-xs font-bold text-slate-400 uppercase">Acute Workload (7 Days)</span>
                        <p className="text-3xl font-black text-emerald-400">{loadMetrics.acuteWorkload} <span className="text-xs text-slate-400">AU</span></p>
                        <p className="text-[11px] text-slate-500">Duration × RPE cumulative load</p>
                      </div>
                      <div className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-2">
                        <span className="text-xs font-bold text-slate-400 uppercase">Chronic Workload (28 Days)</span>
                        <p className="text-3xl font-black text-blue-400">{loadMetrics.chronicWorkload} <span className="text-xs text-slate-400">AU</span></p>
                        <p className="text-[11px] text-slate-500">Target fitness baseline ratio (0.8 - 1.3)</p>
                      </div>
                      <div className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-2">
                        <span className="text-xs font-bold text-slate-400 uppercase">Training Monotony Index</span>
                        <p className="text-3xl font-black text-amber-400">{loadMetrics.monotonyIndex}</p>
                        <p className="text-[11px] text-slate-500">Optimal range: &lt; 2.0 (Prevents staleness)</p>
                      </div>
                      <div className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-2">
                        <span className="text-xs font-bold text-slate-400 uppercase">Weekly Distance Covered</span>
                        <p className="text-3xl font-black text-purple-400">{loadMetrics.weeklyDistanceKm} <span className="text-xs text-slate-400">km</span></p>
                        <p className="text-[11px] text-slate-500">High-speed running: {loadMetrics.highSpeedRunningM}m</p>
                      </div>
                    </div>
                  </div>
                )}

                {activeTab === 'matchday' && (
                  <div className="bg-slate-900 border border-slate-800 rounded-3xl p-6 shadow-2xl space-y-6">
                    <div>
                      <h2 className="text-xl font-black text-white">Interactive Matchday Starting XI & Set-Piece Designer</h2>
                      <p className="text-xs text-slate-400">Plan matchday formations, tactical shapes in possession, and dead-ball routines.</p>
                    </div>

                    <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                      <div className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-4">
                        <h3 className="font-bold text-emerald-400 text-sm">Matchday Starting Lineup (XI)</h3>
                        <div className="space-y-2 max-h-[300px] overflow-y-auto">
                          {roster.slice(0, 11).map((p) => (
                            <div key={p.id} className="flex justify-between items-center bg-slate-900 p-3 rounded-xl border border-slate-800">
                              <span className="text-xs font-bold text-white">#{p.number} {p.name}</span>
                              <span className="text-xs text-emerald-400 bg-emerald-950 px-2.5 py-0.5 rounded border border-emerald-800">{p.pos}</span>
                            </div>
                          ))}
                        </div>
                      </div>

                      <div className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-4">
                        <h3 className="font-bold text-amber-400 text-sm">Set-Piece Tactical Routines</h3>
                        <div>
                          <label className="text-xs font-bold text-slate-400 block mb-1">Attacking Corner Routine</label>
                          <textarea rows="3" value={matchdayXI.cornerRoutine} onChange={e => setMatchdayXI({...matchdayXI, cornerRoutine: e.target.value})} className="w-full bg-slate-900 border border-slate-800 rounded-xl p-3 text-xs text-slate-200"></textarea>
                        </div>
                        <div>
                          <label className="text-xs font-bold text-slate-400 block mb-1">Direct Free Kick Strategy</label>
                          <textarea rows="3" value={matchdayXI.freeKickRoutine} onChange={e => setMatchdayXI({...matchdayXI, freeKickRoutine: e.target.value})} className="w-full bg-slate-900 border border-slate-800 rounded-xl p-3 text-xs text-slate-200"></textarea>
                        </div>
                      </div>
                    </div>
                  </div>
                )}

                {activeTab === 'calendar' && (
                  <div className="bg-slate-900 border border-slate-800 rounded-3xl p-6 shadow-2xl space-y-6">
                    <div className="flex flex-wrap items-center justify-between gap-4">
                      <div>
                        <h2 className="text-xl font-black text-white">Full Year Club Calendar & Match Notes</h2>
                        <p className="text-xs text-slate-400">Select any date to record pre-game tactical objectives and post-game analytical debrief notes.</p>
                      </div>
                      <input type="date" value={selectedCalendarDate} onChange={e => setSelectedCalendarDate(e.target.value)} className="bg-slate-950 border border-slate-700 rounded-xl px-3.5 py-2 text-sm font-bold text-emerald-400" />
                    </div>

                    <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
                      <div className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-4">
                        <h3 className="font-bold text-emerald-400 text-sm">Selected Date: {selectedCalendarDate}</h3>
                        <div className="space-y-3">
                          <div>
                            <label className="block text-xs font-bold text-slate-400 mb-1 uppercase">Pre-Game Tactical Objectives</label>
                            <textarea rows="4" value={calendarNotes[selectedCalendarDate]?.preGame || ''} onChange={e => {
                              const updated = { ...calendarNotes };
                              if (!updated[selectedCalendarDate]) updated[selectedCalendarDate] = {};
                              updated[selectedCalendarDate].preGame = e.target.value;
                              setCalendarNotes(updated);
                            }} className="w-full bg-slate-900 border border-slate-800 rounded-xl p-3 text-xs text-slate-200" placeholder="Enter pre-match tactical game plan..."></textarea>
                          </div>
                          <div>
                            <label className="block text-xs font-bold text-slate-400 mb-1 uppercase">Post-Game Analytical Debrief & GPS Data</label>
                            <textarea rows="4" value={calendarNotes[selectedCalendarDate]?.postGame || ''} onChange={e => {
                              const updated = { ...calendarNotes };
                              if (!updated[selectedCalendarDate]) updated[selectedCalendarDate] = {};
                              updated[selectedCalendarDate].postGame = e.target.value;
                              setCalendarNotes(updated);
                            }} className="w-full bg-slate-900 border border-slate-800 rounded-xl p-3 text-xs text-slate-200" placeholder="Enter match outcome and takeaways..."></textarea>
                          </div>
                          <button onClick={() => { saveToLocalStorage(); showToast(`Saved notes for ${selectedCalendarDate}!`); }} className="bg-emerald-600 hover:bg-emerald-500 text-white text-xs font-bold px-4 py-2.5 rounded-xl shadow w-full">Save Date Notes</button>
                        </div>
                      </div>

                      <div className="bg-slate-950 border border-slate-800 rounded-2xl p-5 space-y-4">
                        <h3 className="font-bold text-slate-300 text-sm">Saved Calendar Entries Archive</h3>
                        <div className="space-y-2 max-h-[360px] overflow-y-auto pr-2">
                          {Object.keys(calendarNotes).length === 0 && <p className="text-xs text-slate-500 italic">No notes recorded yet.</p>}
                          {Object.entries(calendarNotes).map(([date, data]) => (
                            <div key={date} onClick={() => setSelectedCalendarDate(date)} className={`p-3 rounded-xl border cursor-pointer transition ${selectedCalendarDate === date ? 'bg-emerald-950/40 border-emerald-600' : 'bg-slate-900 border-slate-800'}`}>
                              <span className="font-bold text-emerald-400 text-xs">{date}</span>
                              <p className="text-[11px] text-slate-300 truncate">{data.preGame || data.postGame || 'Empty notes'}</p>
                            </div>
                          ))}
                        </div>
                      </div>
                    </div>
                  </div>
                )}

                {activeTab === 'microcycle' && (
                  <div className="bg-slate-900 border border-slate-800 rounded-3xl p-6 shadow-2xl space-y-6">
                    <h2 className="text-xl font-black text-white">Weekly Periodization Micro-Cycle Planner</h2>
                    <div className="grid grid-cols-1 md:grid-cols-7 gap-3">
                      {Object.entries(microcycle).map(([day, data]) => (
                        <div key={day} className="bg-slate-950 border border-slate-800 rounded-2xl p-4 flex flex-col justify-between">
                          <div>
                            <span className="font-black text-emerald-400 text-xs uppercase">{day}</span>
                            <p className="text-xs text-slate-300 mt-2 font-semibold">{data.type}</p>
                            <span className="text-[10px] text-amber-400 mt-1 block">{data.load} Load</span>
                          </div>
                        </div>
                      ))}
                    </div>
                  </div>
                )}

                {activeTab === 'bank' && (
                  <div className="bg-slate-900 border border-slate-800 rounded-3xl p-6 shadow-2xl space-y-6">
                    <h2 className="text-xl font-black text-white">Club Drill Repository & Bank</h2>
                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                      {drillBank.map(drill => (
                        <div key={drill.id} className="bg-slate-950 border border-slate-800 rounded-2xl p-5">
                          <h3 className="font-bold text-white text-base mb-1">{drill.title}</h3>
                          <p className="text-xs text-slate-400">{drill.explanation}</p>
                        </div>
                      ))}
                    </div>
                  </div>
                )}

                {activeTab === 'library' && (
                  <div className="bg-slate-900 border border-slate-800 rounded-3xl p-6 shadow-2xl space-y-6">
                    <h2 className="text-xl font-black text-white">Session Library Archive & Export</h2>
                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                      {sessionLibrary.map(item => (
                        <div key={item.id} className="bg-slate-950 border border-slate-800 rounded-2xl p-5">
                          <h3 className="font-bold text-white text-base mb-1">{item.sheetData.sessionTitle}</h3>
                          <p className="text-xs text-emerald-400">{item.sheetData.clubName}</p>
                        </div>
                      ))}
                    </div>
                  </div>
                )}

              </main>
            </div>
          );
        }

        ReactDOM.createRoot(document.getElementById('root')).render(<App />);
    </script>
</body>
</html>
